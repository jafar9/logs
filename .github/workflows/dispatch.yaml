name: dispatch

on: [repository_dispatch]

jobs:
  manual_deploy:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v1
        if: contains(github.event, "dkube")
      - name: respository dispatch testing
        env:
          DISPATCH_PAYLOAD: ${{ toJson(github.event) }} 
        run: |
             echo "$DISPATCH_PAYLOAD"
